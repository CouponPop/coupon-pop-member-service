server:
  port: 8081

management:
  endpoints.web.exposure.include: health,info
  endpoint.health.show-details: always

client:
  notification-service:
    name: notification-service
    url: ${NOTIFICATION_SERVICE_URL:http://localhost:8084/internal}